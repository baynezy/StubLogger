on:
  workflow_call:
    inputs:
      deploy-env:
        required: true
        description: 'Environment to deploy to'
        type: string
      deploy-branch:
        required: true
        description: 'Branch to deploy'
        type: string
      version:
        description: 'The version number to use for the build'
        required: true
        type: string
      package-name:
        description: 'The name of the package to push'
        required: true
        type: string
    secrets: 
      nuget-api-key:
        description: 'API Key for NuGet'
        required: true

jobs:
  push-package:
    runs-on: ubuntu-latest
    environment: ${{ inputs.deploy-env }}
    steps:
      - name: Download Package
        uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3 # v6
        with:
          name: ${{ inputs.package-name }}
          path: ./src/StubLogger/bin/Release
      - name: Setup .NET Core
        uses: actions/setup-dotnet@baa11fbfe1d6520db94683bd5c7a3818018e4309 # v5
        with:
          dotnet-version: 8.0.x
      - name: Push package to NuGet
        env:
          NUGET_API_KEY: ${{ secrets.nuget-api-key }}
        run: |
          dotnet nuget push $(find ./src/StubLogger/bin/Release/*.nupkg) --api-key $NUGET_API_KEY --source https://api.nuget.org/v3/index.json